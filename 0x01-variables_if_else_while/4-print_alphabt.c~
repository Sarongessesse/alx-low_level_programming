#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
*main - except
*
*
* Return: 0
*/
int main(void)
{
  char i;
  for (i = 'a' ; i <= 'z' ; i++)
    {
      if (i !='a' && i != 'q')
    putchar (i);
  putchar ('\n');
    }
  return (0);
}
